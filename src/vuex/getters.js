export function getCount(state) {
  return state.count;
}

export function getProjects(state) {
  return state.projects;
}

export function getTasks(state) {
  return state.tasks;
}

export function getChosenProject(state) {
  return state.chosenProject;
}
